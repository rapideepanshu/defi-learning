import { ethers } from "ethers";
import { DAI_WHALE } from "./config";

describe("TestUniwapOptimal", () => {
  const WHALE = DAI_WHALE;
  const AMOUNT = ethers.BigNumber.from("1000000000000000000000");

  let contract, from, to, pair;
});
